node_modules/
dist/
build/

# Never commit. .env.example is the committed template.
.env
.env.*
!.env.example

# Fixture audio is generated or recorded locally, not versioned.
fixtures/*.wav
fixtures/*.aiff

# Attempt records carry device context and learner scores.
server/data/
fixture-separation.png

.DS_Store
*.log

# Local test output
french-activity-test-results.json
synthetic-fixture.json
out.json
